// describe('auth route', () => {
//     describe('POST /auth/login', () => {
//         it('should respond with a message (and 200) if login is successful', () => {

//         });
//         it('should respond with Unauthorized (400?) if the login fails', () => {

//         });
//     });
//     describe('POST /auth/register', () => {
//         it('should respond with a message (and 200) if registration is successful', () => {

//         });
//         it('should respond with a message (and 500) if registration fails', () => {
            
//         });
//     });
//     describe('POST /auth/reset', () => {
//         it('should respond with a message (and 200) if reset request is successful', () => {

//         });
//         it('should respond with a message (and 500) if reset request fails', () => {

//         });
//     });
//     describe('GET /auth/reset/:email/:token', () => {
//         it('should render index with resetPassword: true, if the token is valid', () => {

//         });
//         it('should render error with a messsage if the token is not valid', () => {

//         });
//     });
//     describe('POST /auth/reset/:email/:token', () => {
//         it('should respond with a message (and 200) if the password was successfully changed', () => {

//         });
//         it('should respond with a message (and 422) if the token is invalid', () => {

//         });
//     });
// });
